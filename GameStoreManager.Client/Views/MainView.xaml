<Window x:Class="GameStoreManager.Client.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameStoreManager.Client.Views"
        mc:Ignorable="d"
        Title="Strona główna" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Center" FontSize="24" VerticalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center">Dodawanie, usuwanie i zarządzanie ofertami sprzedaży</TextBlock>
        <Button Command="{Binding GoToAuctionManagerCommand}" Grid.Column="0" Grid.Row="1" Margin="10" FontSize="24">Zarządzaj aukcjami</Button>
        <TextBlock HorizontalAlignment="Center" FontSize="32" VerticalAlignment="Center" Grid.Column="1" Grid.Row="0" TextWrapping="Wrap" TextAlignment="Center">Dane dotyczące sprzedaży</TextBlock>
        <Button Command="{Binding GoToSalesManagerCommand}" Grid.Column="1" Grid.Row="1" Margin="10" FontSize="20">Wgląd w dane sprzedaży</Button>

    </Grid>
</Window>
